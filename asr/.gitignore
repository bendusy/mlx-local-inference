__pycache__/
*.pyc
.venv/
.pytest_cache/
data/
*.egg-info/
scripts/eval_service.log

# Tests live locally only — not part of the published module.
# Run with `cd asr && uv run pytest` if you cloned with full history.
tests/
logs/
